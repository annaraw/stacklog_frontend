import { makeStyles } from "@material-ui/core";
import { Colors } from "../../../util/constants";

export const loginDataStyles = makeStyles({
    textField: {
        marginBottom: "15px",
    },
    button: {
        width: "100%",
        backgroundColor: Colors.primaryColor
    },
});